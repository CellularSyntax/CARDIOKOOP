# CARDIOKOOP — Revision 3 notes (journal *Array*, minor revision; release v1.1.0)

Purpose of this document: give every number, table and figure in the manuscript
"Real-Time Hemodynamic Prediction via Control-Aware Koopman Operator Models" a committed
file of origin and the script that regenerates it, and document the conventions and
corrections introduced in revision 3.

Evaluation split for every reported number: the seed-42 **test1** split
(`data/csv_data_500_12sigs_test1_{x,u}.csv`; 50 trajectories × 1500 steps × 12 signals,
1499-step autoregressive horizon). Frozen Koopman checkpoint:
`results/koopman/csv_data_500_12sigs_2025_06_18_18_50_13_876051_model.ckpt` (17,239,600 parameters).
Statistical unit: the test trajectory (n = 50); 95 % CI = 1.96·SD/√50 across trajectories.

---

## 1. Table / figure / number → script → result file

| Manuscript item | Script (reproduce with) | Committed result file(s) |
|---|---|---|
| **Table 3** (R², %RMSE, inference time, speed-up, parameters, training time) | `scripts/revision3/export_manuscript_tables.py` | `results/revision3/table3_overall.{md,json,tsv}` |
| **Table 4** (per-signal RMSE, %RMSE, R², Bland–Altman bias/LoA; all 8 models) | `scripts/revision3/export_manuscript_tables.py` | `results/revision3/table4_per_signal_full.{md,json,tsv}` |
| **Table 5** (AWGN robustness, %RMSE and R², 8 models × 5 conditions) | `scripts/revision3/export_manuscript_tables.py` (clean row) ← `scripts/task3_noise_robustness.py`, `scripts/task3b_noise_robustness_baselines.py`, `scripts/revision2/build_tables_figures.py` (noisy rows) | `results/revision3/table5_noise.{md,json,tsv}`; sources `results/noise_robustness.json`, `results/noise_robustness_baselines.json`, `results/revision2/noise_robustness_linear.json` |
| Statistics (Shapiro–Wilk, Friedman, Wilcoxon + Bonferroni; Table 3 note, Fig. 5b caption, Methods, Results) | `scripts/revision3/export_manuscript_tables.py` | `results/revision3/statistics.json` |
| Per-trajectory R² statements (Results, Fig. 5g caption, Introduction/Discussion "R² < 0" counts) | `scripts/revision3/export_manuscript_tables.py` | `results/revision3/r2_conventions.json` |
| Koopman test1 predictions and metrics (pickle, same keys as the baseline pickles) | `scripts/revision3/export_manuscript_tables.py` | `results/revision3/koopman_test1_postprocessing_results.pkl` |
| Inference times / speed-ups (Table 3, Fig. 5c,e) | measured on the original workstation when the checkpoints were produced; re-used verbatim in `scripts/revision2/build_tables_figures.py` and `scripts/revision3/export_manuscript_tables.py` (`PUB_INF`, `PUB_SPD`, `PUB_SPD_LIN`) | `results/revision2/table3_overall_comparison.csv` |
| Training times (Table 3) | `scripts/measure_training_times.py` (MLP, AR); Koopman/RNN from the training logs (`results/*/*_error.csv`) | `results/training_times.json`, `TRAIN_TIMES_MIN` in `build_tables_figures.py` |
| Parameter counts (Table 3, Fig. 5f) | `scripts/check_ckpt_params.py`; `n_params` in each pickle | `results/*/*_postprocessing_results.pkl`, `checkpoints/*.pt` |
| **Figure 5** a, g–j (example rollouts, R² violins, per-signal R² heatmap, noise panels) | `scripts/revision2/regenerate_panels_a_gj.py` (after `build_tables_figures.py`) | `results/figures/figure5a_rev2.svg`, `results/figures/figure5_panels_gj_rev2.svg` |
| **Figure 5** b–f (per-signal %RMSE, accuracy vs. time, cumulative %RMSE, speed-up, model size) | `scripts/revision2/build_tables_figures.py` | `results/figures/figure5_rev2_comparison.svg`, `results/figures/figure6_rev2_noise.svg` (noise curves) |
| Figures 2, 3, 4 (parameter space + spectra, Koopman training/latent analysis, full-horizon prediction) | `notebooks/generate_figures_and_tables_revised_rev2.ipynb` (+ `notebooks/notebook_utils.py`) | `results/figures/figure2.svg`, `figure3a-c.svg`, `figure3d.svg`, `figure4.svg` |
| Figure 1 (method overview) | schematic, not generated by code | — |
| Fig. S1–S3, Table S1–S2 (Optuna search overview, importance, parallel plot; search space; best configuration) | `notebooks/generate_figures_and_tables_revised_rev2.ipynb`; `src/cardiokoop/optim/optuna_koopman_search.py` | `results/optuna_runs/koopman_hyperparam_search_20250713_193701.csv`, `results/figures/supplementary_figure{1,2,3}.svg` |
| Table S3 (LHS design distances / leakage check) | `scripts/task2_lhs_distance.py` | `results/lhs_distance_check.json`, `results/table_S3_lhs_distances.{csv,tex}` |
| Fig. S4 (latent-space / hyper-parameter sensitivity) | `scripts/task6_latent_ablation.py`, `scripts/revision2/figure_latent_hparam.py` | `results/latent_ablation.json`, `results/figures/figureS4.svg` |
| Fig. S5 (all 12 signals, physical and latent space) | `notebooks/generate_figures_and_tables_revised_rev2.ipynb` | `results/figures/figureS_all_signals.svg` |
| Fig. S6 + mode-ablation table (Koopman latent mode analysis) | `scripts/task8_mode_analysis.py`, `scripts/revision2/task_e_mode_ablation_export.py`, `scripts/revision2/figure_mode_analysis.py` | `results/mode_frequencies.json`, `results/mode_ablation.json`, `results/revision2/table_mode_ablation.{csv,tex}`, `results/figures/figureS6_mode_analysis.svg` |
| Fig. S7 (control-gain γ sweep) | `scripts/revision2/task_b_gamma_sweep.py` | `results/revision2/task_b_gamma_sweep.csv`, `results/figures/figureS_gamma_control_sweep.svg` |
| Table S4 (control-net activation comparison) | `scripts/revision2/task_c_activation.py` | `results/revision2/task_c_activation.{csv,json,tex}` |
| Fig. S8, Table S6 (realistic + streaming noise) | `scripts/revision2/task_d_realistic_streaming_noise.py` | `results/revision2/task_d_realistic_streaming_noise.{csv,json}`, `results/figures/figureS_realistic_streaming_noise.svg` |
| Table S5 (baseline configurations) | `src/cardiokoop/training/train_{lstm,gru,bilstm}.py`, `scripts/task5_mlp_baseline.py`, `scripts/task4_ar_baseline.py`, `scripts/revision2/task_a_dlinear_nlinear.py` | `results/*/*.ckpt` (`ckpt["params"]`), `results/mlp_baseline.json`, `results/ar_baseline.json`, `results/revision2/task_a_dlinear_nlinear.json` |
| Fig. S9 (full eight-architecture version of Fig. 5) | `scripts/revision2/build_tables_figures.py`, `scripts/revision2/regenerate_panels_a_gj.py` | `results/figures/figureS9_comparison_full8.svg`, `figureS9a_full8.svg`, `figureS9_panels_gj_full8.svg` |
| Fig. S10 (closed-loop re-anchoring) | `scripts/revision2/task_f_clean_reanchoring.py` | `results/revision2/task_f_clean_reanchoring.csv`, `results/figures/figureS_clean_reanchoring.svg` |
| γ-sensitivity numbers (Discussion) | `scripts/task7_gamma_sensitivity.py` | `results/gamma_sensitivity.json` |
| Abstract timings (5 ms vs 640–1600 ms, ~380×) | Table 3 sources above | `results/revision3/table3_overall.json` |

Everything in `results/revision3/` is produced by **one** command (about one minute on CPU):

```bash
python scripts/revision3/export_manuscript_tables.py
```

---

## 2. R² conventions (definitions in `results/revision3/r2_conventions.json`)

Three R² definitions occur in the manuscript; revision 3 states explicitly which is used where.

| Convention | Definition | Used in |
|---|---|---|
| **Pooled per-trajectory R²** | 1 − SS_res/SS_tot over all 12 signals × 1500 steps of one trajectory concatenated (`_common.r2_flat`); reported as mean ± 95 % CI over the 50 trajectories | Table 3, Table 5, Fig. 5g, Fig. S10 caption, Table S4 |
| **Per-signal R²** | `sklearn.metrics.r2_score` of one signal of one trajectory; mean ± 95 % CI over trajectories | Table 4, Fig. 5h |
| **Signal-averaged per-trajectory R²** | mean of the 12 per-signal R² of one trajectory (`r2_per_trajectory` key of the pickles) | "trajectories with R² < 0" statements of the per-signal analysis |

Test1 results (`r2_conventions.json`):

| Model | Pooled R² (mean ± CI) | trajectories with pooled R² < 0 | Signal-averaged R² (mean, SD) | trajectories with signal-averaged R² < 0 |
|---|---|---|---|---|
| Koopman | 0.69 ± 0.03 (min 0.45) | 0/50 | 0.36 (0.76) | 6/50 (driven by P_vp, mean R² −2.16) |
| LSTM | −0.59 ± 0.11 | 47/50 | −21.1 (19.6) | 50/50 |
| GRU | −0.31 ± 0.07 | 45/50 | −5.09 (2.77) | 50/50 |
| BiLSTM | −0.30 ± 0.07 | 44/50 | −13.2 (16.2) | 50/50 |
| MLP | −17.6 ± 10.5 (median 0.12; 11/50 rollouts diverge) | 20/50 | −114 (284) | 38/50 |
| AR(20) | 0.33 ± 0.01 | 0/50 | −0.62 (0.51) | 50/50 |
| DLinear | 0.98 ± 0.00 | 0/50 | 0.96 (0.07) | 0/50 |
| NLinear | 0.98 ± 0.00 | 0/50 | 0.95 (0.07) | 0/50 |

The revision-1 Table 3 R² row (Koopman 0.74, LSTM −6.5, GRU −1.46, BiLSTM −2.68, AR(20) 0.06, MLP −38.53) mixed
conventions and splits and is not reproducible from any committed file; it is replaced by the pooled row above, so that
Table 3 and the clean row of Table 5 are identical by construction. The per-signal Koopman R² (mean over signals of the
Table 4 values) is 0.36; the manuscript now reports 0.69 (pooled) as the headline value.

---

## 3. Validation-vs-test pickle issue and its resolution

* `results/koopman/koopman_postprocessing_results.pkl` (revision 1, source of the original Table 4 Koopman row) was
  generated on the **val1** split (mean %RMSE 17.09 with the pickle's own averaging; val 17.30 / test 17.54 with the
  revision-2 metric functions). Every baseline pickle (`results/{lstm,gru,bilstm,dlinear,nlinear}/`) is test1.
* Resolution: `scripts/revision3/export_manuscript_tables.py` rolls the frozen checkpoint out on **test1** and writes
  `results/revision3/koopman_test1_postprocessing_results.pkl` with the same keys as the baseline pickles
  (`true_per_trajectory`, `pred_per_trajectory`, `pct_per_traj_ps`, `r2_per_signal`, …, plus `split`, `checkpoint`,
  `rollout_dtype`, `device`). Table 4 (Koopman row) and all Koopman statistics are computed from this test1 evaluation.
  The revision-1 val1 pickle is kept unchanged for provenance and is not used by any revision-2/3 table.
* MLP and AR(20) rows of the revision-1 Table 4 were produced by older code paths (different clipping / fitting details);
  they are regenerated with the revision-2 functions (`build_tables_figures.mlp_pred`: committed
  `checkpoints/mlp_baseline.pt`, autoregressive rollout with clip ±20 in normalised units; `build_tables_figures.ar_pred`:
  OLS AR(20) per signal fitted on train1). Largest changes: MLP P_vp %RMSE 424 → 356, AR(20) P_vp 68 → 39.
* LSTM, GRU, BiLSTM, DLinear, NLinear rows are read from the committed pickles and are unchanged (≤ 0.1 rounding).
* The script asserts that every baseline pickle's `true_per_trajectory` equals the test1 ground truth.

---

## 4. Statistics recomputation (`results/revision3/statistics.json`)

Revision-1 `results/stats_exact.json` covered four models (Koopman, LSTM, GRU, BiLSTM; Friedman χ² = 107.57), while the
Methods describe an omnibus test over the six dynamical models with five Bonferroni-corrected comparisons; the reported
"Bonferroni-adjusted p = 8.88 × 10⁻¹⁶" was the unadjusted one-sided floor. Recomputed on the per-trajectory %RMSE
(`pct_per_traj_ps`, n = 50) of the Table 3 predictions:

* Shapiro–Wilk: p < 0.05 for all six dynamical models (Koopman W = 0.82, p = 3.2×10⁻⁶; LSTM p = 0.038; GRU p = 0.014;
  BiLSTM p = 1.2×10⁻³; MLP p = 3.0×10⁻¹⁰; AR(20) p = 2.4×10⁻³) → non-parametric tests.
* Friedman (Koopman, LSTM, GRU, BiLSTM, MLP, AR(20); k = 6, n = 50): **χ² = 175.55, df = 5, p = 4.8 × 10⁻³⁶**.
* Paired two-sided exact Wilcoxon signed-rank, Koopman vs. baseline, Bonferroni × 5:
  GRU, LSTM, BiLSTM, AR(20): W = 0, 50/50 trajectories worse than Koopman, **p = 1.78 × 10⁻¹⁵ (adjusted 8.9 × 10⁻¹⁵)**;
  MLP: W = 20, 47/50 worse, **p = 6.6 × 10⁻¹³ (adjusted 3.3 × 10⁻¹²)**.
* DLinear/NLinear are direct (non-autoregressive) forecasters and are not part of the omnibus test.

Suggested Methods sentence: "The statistical unit throughout is the test trajectory (n = 50). All 95 % CIs are
1.96·SD/√50 across trajectories (also the per-signal CIs of Table 4, computed from the 50 per-trajectory per-signal
values); hypothesis tests are paired across trajectories."

---

## 5. Table 5 clean row

The clean row of Table 5 is, by construction, identical to Table 3 (same test1 predictions). The noise harnesses'
own clean baselines (`clean_baseline` keys of `results/noise_robustness_baselines.json`) differ marginally for the RNNs
(LSTM 96.8 vs 97.8; BiLSTM 77.2 vs 76.4; GRU R² −0.34 vs −0.31) because of warm-up handling; the noisy rows are taken
from the JSONs unchanged. Koopman noise rows: `results/noise_robustness.json` (`scripts/task3_noise_robustness.py`);
DLinear/NLinear: `results/revision2/noise_robustness_linear.json` (`scripts/revision2/build_tables_figures.py`).

---

## 6. CPU-vs-GPU platform note

The 1499-step autoregressive rollout amplifies floating-point differences. For the frozen checkpoint on test1:

| Platform | Koopman mean %RMSE | 95 % CI | pooled R² |
|---|---|---|---|
| authors' workstation, GPU (RTX 3070 Ti), float32 — `results/revision2/table3_overall_comparison.csv` (revision 2) | 17.5 (17.54) | 1.44 | 0.691 (global pooled) |
| authors' workstation, GPU, float32 — `results/noise_robustness.json` clean baseline (revision 1) | 17.54 | 1.49 | 0.686 (mean per-trajectory pooled) |
| CPU, float32 | 17.46 | — | 0.69 |
| CPU, float64 — committed `results/revision3/*` | 17.50 | 1.47 | 0.688 (mean per-trajectory pooled); 0.692 (global pooled) |

All values round to 17.5 %; per-signal Koopman values agree to ≤ 0.1 pp; W = 0 for all four Wilcoxon comparisons on
every platform; the RNN/DLinear/NLinear predictions are read from the committed pickles and are bit-identical everywhere.
`results/revision3/run_info.json` records the platform of the committed run (CPU, float64, torch 2.12.1). The authors
can regenerate `results/revision3/` on the original workstation with the same command; the manuscript quotes the
authors' GPU CI "17.5 ± 1.4" (revision-2 run), the CPU-f64 run gives 17.5 ± 1.5.

---

## 7. Release / archive

* GitHub: tag `v1.1.0` (release "v1.1.0 — Array revision reproducibility package").
* Zenodo software archive v1.1.0: DOI 10.5281/zenodo.22771300 (concept DOI 10.5281/zenodo.21993894). Unlike v1.0.0
  (GitHub-integration snapshot with Git-LFS pointer files), the v1.1.0 archive contains the resolved content of all 523
  `*.csv` files (≈ 0.9 GB uncompressed) plus a `MANIFEST.sha256`.
* Dataset record (splits only): DOI 10.5281/zenodo.21163127.
* Files changed in this release relative to v1.0.0: `README.md` (rewritten), `pyproject.toml` (version 1.1.0); new:
  `CITATION.cff`, `REVISION3_NOTES.md`, `environment/`, `scripts/revision3/`, `results/revision3/`. No existing file
  other than `README.md` and `pyproject.toml` was modified or removed.

---

## 8. Container verification (Docker image + GitHub Actions, v1.2.0)

**What runs.** `Dockerfile` (python:3.11-slim, CPU wheel of torch 2.6.0, every pin of `environment/requirements-pinned.txt`,
non-editable install of the package, the frozen checkpoints and the committed results) with `scripts/reproduce.sh` as default
command. The workflow `.github/workflows/reproduce.yml` (push to `main`/`docker-ci`, pull requests, `v*` tags, manual) builds the
image, runs the container with the repository mounted **read-only** (only `out/` writable), and:

1. downloads `csv_data_500_12sigs_test1_{x,u}.csv` and `csv_data_500_12sigs_train1_x.csv` from the Zenodo dataset record
   10.5281/zenodo.21163127 (record 21163128) because `data/*.csv` are Git-LFS pointers in a plain checkout, and verifies their MD5
   against the record (`normalization_{mean,std}.npy` are plain files in the repository and are MD5-verified as well; the Zenodo
   files are byte-identical to the LFS objects — SHA-256 of the downloads equals the LFS `oid`);
2. runs `scripts/revision3/export_manuscript_tables.py --out-dir /workspace/out` (float64 Koopman rollout on CPU; all 50 test
   trajectories, 1499 steps) — Koopman rollout ≈ 42 s, MLP re-roll check ≈ 15 s, AR(20) OLS + rollout ≈ 10 s on the 4-vCPU runner;
3. runs `scripts/revision3/compare_results.py --committed results/revision3 --fresh /workspace/out`, uploads `out/` as the
   workflow artifact `reproduction-tables`, and on `main` / `v*` pushes the verified image to `ghcr.io/cellularsyntax/cardiokoop`
   (tags `sha-<short>`, `latest`, `v*`; digest in `out/image_digest.txt` and in the job summary).

**Pass criteria** (`compare_results.py`, 1467 metrics): every number is compared *as printed in the manuscript* — Table 3 %RMSE and
CI to 1 decimal, pooled R² and CI to 2 decimals, parameter counts / inference times / speed-ups exact; Table 4 RMSE, %RMSE, CIs,
bias and LoA to 1 decimal, R² and CI to 2 decimals (8 models × 12 signals × 9 values); Table 5 %RMSE to 1 decimal, R² to 2
decimals (clean row recomputed, noisy rows read from the committed JSONs); statistics: Friedman χ² to 2 decimals, Shapiro W to
3 decimals, every p-value within a factor of 2, Wilcoxon W to the integer, trajectory counts (worse/better/ties, negative-R²
counts) exact, summary statistics to 1 decimal; `r2_conventions.json` means/SDs/CIs/medians/min/max to 2 decimals, global pooled
R² to 3 decimals. A change of exactly one unit in the last printed digit is tolerated (unless `REPRO_STRICT=1`) but is listed in
`out/compare_report.md`; anything larger fails the job.

**Finding: the float32 MLP rollout is not platform-independent at manuscript rounding.** The first container run
(GitHub Actions run 35010585147, x86-64 `Linux-6.17-azure`, torch 2.6.0+cpu, numpy 2.2.6) reproduced **every** Koopman, LSTM, GRU,
BiLSTM, AR(20), DLinear and NLinear number and every statistic identically at printed precision (Friedman χ² = 175.55, all
Wilcoxon p-values, W = 20 and 47/50 for the MLP comparison) — 1330 of 1459 metrics — but all remaining 129 metrics
(95 beyond rounding, 34 off-by-one) belong to the MLP baseline, whose autoregressive float32 rollout with clip ±20 diverges
(%RMSE > 100 %) and therefore amplifies CPU-architecture floating-point differences (Apple Silicon/Accelerate vs. x86-64/MKL):

| MLP metric (Table 3 / Table 5 clean) | committed (macOS arm64, torch 2.12.1) | x86-64 container re-roll |
|---|---|---|
| %RMSE mean ± 95 % CI | 132.2 ± 55.1 (132.24 / 55.15) | 132.8 ± 55.2 (132.75 / 55.24) |
| pooled R² mean ± 95 % CI | −17.63 ± 10.45 | −17.73 ± 10.46 |
| global pooled R² | −18.688 | −18.830 |
| trajectories with pooled R² < 0 | 20 | 19 |
| Wilcoxon Koopman vs. MLP | W = 20, p = 6.59e-13, 47/50 worse | identical |

| Signal | MLP Table 4 cells that differ (committed → x86 container re-roll; ±1 = last-digit rounding only) |
|---|---|
| P_ra | pct_rmse 187.4→188.3; pct_rmse_ci95 82.0→82.3; r2 -253.98→-255.50; r2_ci95 145.64→145.81 |
| V_ra | rmse 35.6→35.7 (±1); pct_rmse 79.8→80.0; r2 -22.28→-22.42; r2_ci95 11.90→11.91 (±1); bias -15.0→-15.1 (±1); loa_lower -60.3→-60.4 (±1); loa_upper 30.3→30.2 (±1) |
| P_rv | rmse 51.0→51.3; pct_rmse 147.2→147.9; pct_rmse_ci95 64.8→64.9 (±1); r2 -48.62→-48.94; r2_ci95 28.27→28.31; bias -37.0→-37.3; loa_lower -92.9→-93.4 |
| V_rv | rmse 134.0→134.5; rmse_ci95 55.9→56.1; pct_rmse 139.3→139.8; pct_rmse_ci95 60.7→60.8 (±1); r2 -82.01→-82.35; r2_ci95 48.34→48.36; bias 86.9→87.5; loa_lower -49.6→-49.3; loa_upper 223.5→224.4 |
| P_vp | rmse 14.9→15.0 (±1); pct_rmse 356.2→357.6; pct_rmse_ci95 198.1→198.3; r2 -619.69→-621.49; r2_ci95 582.19→582.34; bias 9.2→9.3 (±1); loa_upper 23.9→24.0 (±1) |
| P_la | rmse 9.0→9.1 (±1); rmse_ci95 3.0→3.1 (±1); pct_rmse 89.5→89.9; pct_rmse_ci95 35.3→35.4 (±1); r2 -30.93→-31.16; r2_ci95 25.88→25.92; loa_upper 15.1→15.2 (±1) |
| V_la | rmse 66.5→66.8; rmse_ci95 28.7→28.8 (±1); pct_rmse 163.3→163.9; pct_rmse_ci95 74.2→74.4; r2 -151.23→-152.23; r2_ci95 92.96→93.10; bias 44.9→45.3; loa_lower -23.6→-23.4; loa_upper 113.4→113.9 |
| P_lv | rmse 47.1→47.3; pct_rmse 40.0→40.1 (±1); pct_rmse_ci95 6.2→6.3 (±1); r2 -0.55→-0.56 (±1); bias -15.0→-15.2; loa_lower -96.4→-96.7; loa_upper 66.3→66.4 (±1) |
| V_lv | rmse 140.9→141.5; rmse_ci95 56.6→56.8; pct_rmse 168.0→168.7; pct_rmse_ci95 71.0→71.1 (±1); r2 -93.36→-93.78; r2_ci95 56.21→56.23; bias 75.7→76.3; loa_lower -70.6→-70.4; loa_upper 221.9→223.1 |
| Q_i_lv | rmse 548.2→550.3; rmse_ci95 233.2→234.0; pct_rmse 90.9→91.2; pct_rmse_ci95 37.7→37.8 (±1); r2 -50.17→-50.51; r2_ci95 28.81→28.87; bias -393.0→-395.7; loa_lower -1010.0→-1014.0; loa_upper 224.1→222.6 |
| Q_o_lv | rmse 291.6→292.7; rmse_ci95 79.7→79.9; pct_rmse 63.2→63.4; r2 -6.35→-6.38; bias -152.4→-153.5; loa_lower -552.5→-554.7; loa_upper 247.7→247.6 (±1) |
| P_ao | pct_rmse 62.0→62.1 (±1); r2 -9.07→-9.08 (±1); bias 4.2→4.3 (±1); loa_upper 47.9→48.0 (±1) |

(`P_ao`: pct_rmse 62.0→62.1, r2 −9.07→−9.08, bias 4.2→4.3, loa_upper 47.9→48.0, all ±1. The full 129-row list is in the
verification report of the docker-ci branch run.)

**Resolution (v1.2.0).** The MLP *predictions* of the committed run are frozen in `results/mlp/mlp_postprocessing_results.pkl`
(same keys and convention as the GRU/LSTM/BiLSTM/DLinear/NLinear pickles; generated with `build_tables_figures.mlp_pred` on the
committed platform and **bit-identical** to the predictions behind `results/revision3/` — no committed table value changed).
`export_manuscript_tables.py` loads them by default (`--recompute-mlp` re-rolls the checkpoint instead), so the Table 3/4/5 MLP
rows are now reproduced exactly on every platform, and the MLP row has a committed file of origin like every other baseline. On
every run the checkpoint is nevertheless re-rolled in float32 and float64 and compared with the frozen predictions
(`mlp_recompute_check.json`); the float32 re-roll is gated at 2 % relative on mean %RMSE and pooled R² (observed on x86-64:
+0.39 % and −0.57 %), the float64 re-roll is informational. Notably the **float64** MLP re-roll is platform-independent
(133.6309060 on arm64 vs. 133.6309062 on x86-64, pooled R² −17.6786 on both) but differs from the manuscript's float32 rollout by
+1.0 pp %RMSE; switching the MLP baseline to float64 would therefore be a clean alternative for a future revision at the cost of
changing the MLP row.

**Final verification run (docker-ci branch, commit 12a3ed9, run 35012431235):** 1467 metrics, **1463 identical at printed
precision, 0 off-by-one, 0 violations**, 4 informational (float64 re-roll and bit-identity flags). Container wall time 98 s
(data download 88 s, export 79 s on a cached second run), total job ≈ 7 min including the image build (≈ 4.5 min, GHA-cached).

**Note for the manuscript.** The check verifies the repository against `results/revision3/`. The Table 3 / Table 5 clean MLP
values in the current manuscript draft (132.7 ± 55.2, −17.73 ± 10.46) and the Koopman CI (17.5 ± 1.4) are the revision-2
workstation (GPU, float32) values, not the committed revision-3 values (132.2 ± 55.1, −17.63 ± 10.45, 17.5 ± 1.5); 18 further
Table 4 cells of the LSTM/GRU/BiLSTM rows differ from `table4_per_signal_full.json` in the last printed digit. These
manuscript-side discrepancies are outside the scope of the container check and are to be aligned in the final submission files.
