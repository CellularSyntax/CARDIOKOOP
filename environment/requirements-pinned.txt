# CARDIOKOOP v1.2.0 — pinned Python environment for all reported result runs
# (Array, 2026: "Real-Time Hemodynamic Prediction via Control-Aware Koopman Operator Models").
#
# PyTorch 2.6 and Optuna 4.7.0 as stated in the manuscript; every other package is pinned to a
# PyPI release compatible with PyTorch 2.6.
#
# Install (CUDA build of torch 2.6.0 from the PyTorch index, e.g. cu124):
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#   pip install -r environment/requirements-pinned.txt
#   pip install -e .
# The CPU-only build (`pip install torch==2.6.0`) reproduces every number to <= 0.1 pp %RMSE
# (see README.md, "Reproducibility notes", and docs/REPRODUCIBILITY.md).
torch==2.6.0
optuna==4.7.0
numpy==2.2.6
scipy==1.15.2
pandas==2.2.3
scikit-learn==1.6.1
statsmodels==0.14.4
matplotlib==3.10.1
seaborn==0.13.2
plotly==6.0.1
kaleido==0.2.1
fastdtw==0.3.4
tqdm==4.67.1
notebook==7.3.3
